<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lgy.smile.dao.NotificationMapperInterface">

	<select id="count" resultType="int" parameterType="hashmap">
				SELECT 
					COUNT(identity)
				FROM 
					smile.notification
				WHERE user = #{user};	
	</select>

  	<select id="list" resultType="com.lgy.smile.dto.NotificationDto" parameterType="hashmap">
				SELECT 
					notification.*,
				    user.nickname
				FROM 
					smile.notification notification,
				    smile.user user
				WHERE 
					notification.user = #{user}
					AND notification.user = user.identity
  	</select>  	

	<insert id="create" parameterType="hashmap">
				INSERT INTO 
					smile.notification(identity, msg, created, url_path, user) 
				VALUES(
					null, #{msg} ,#{created} , #{url_path}, #{user}
				)
	</insert>  	

	<delete id="delete" parameterType="hashmap">
				DELETE FROM smile.notification 
				WHERE identity = #{identity}
	</delete>  	
  	
</mapper>